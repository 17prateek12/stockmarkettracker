import React from 'react';

const Prediction = ({ selectedSymbol }) => {
  return (
    <div className='predictionmain'>
      <h1>Prediction for {selectedSymbol}</h1>
    </div>
  );
};

export default Prediction;
